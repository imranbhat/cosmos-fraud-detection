apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-feature-store
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cosmos-fraud-detection.serviceLabels" (list . "feature-store-service") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "cosmos-fraud-detection.serviceSelectorLabels" (list . "feature-store-service") | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
