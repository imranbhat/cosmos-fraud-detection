apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-scoring
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cosmos-fraud-detection.serviceLabels" (list . "scoring-service") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "cosmos-fraud-detection.serviceSelectorLabels" (list . "scoring-service") | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
